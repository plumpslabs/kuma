{
  "name": "@kuma/studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Kuma Studio — web-based knowledge graph visualizer & dashboard (bundled with @plumpslabs/kuma).",
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean --out-dir dist",
    "start": "node dist/index.js",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
